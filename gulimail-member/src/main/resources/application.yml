spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://192.168.228.128:3306/gulimail-ums
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.228.128:8848
        ip: localhost
  application:
    name: gulimail-member


mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto

server:
  port: 8001

